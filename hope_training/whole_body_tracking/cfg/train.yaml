# =============================================================================
# Main Hydra config for HOPE Agibot A3 WBC training (106B-Final-Project style).
#
# Usage:
#   python scripts/train.py task=HOPEPingPongDeployParity algo=ppo headless=true \
#       registry_name=<entity>/wandb-registry-motions/hope_forehand
#
# Pick the task/algo YAML with `task=...` / `algo=...`; override any field on the
# CLI (e.g. num_envs=2048 max_iterations=20000 seed=1 task.rewards.racket_position_weight=5).
# =============================================================================

defaults:
  - task: HOPEPingPongDeployParity
  - algo: ppo
  - _self_

headless: true
device: cuda:0
seed: 0

# Override the task defaults when set (null -> use the task/algo YAML value).
num_envs: null
max_iterations: null
motion_file: null       # optional local .npz reference or list; takes precedence over registry_name
motion_file_2: null     # optional 2nd local clip for unified HITTER policy; used only with motion_file
registry_name: null     # falls back to task.registry_name when null
registry_name_2: null   # optional 2nd clip for the unified HITTER policy (backhand); falls back to task.registry_name_2
run_name: null
# Optional fresh-lineage plant control. The checked-in A3 config keeps historical, uncalibrated
# PhysX joint-friction coefficients. Set `task.plant.zero_joint_friction=true` to train the
# zero-friction control that formal MuJoCo BankExam can reproduce exactly. Absent or false preserves
# the configured plant byte-for-byte. Never use this to resume/launder a legacy checkpoint; the
# schema-v3 contract records the instantiated per-joint coefficients.
# Resume / curriculum hand-off: load model+optimizer from a prior run's checkpoint and CONTINUE training
# (iteration counter + wandb step continue from the checkpoint). null -> fresh init. Use this to apply a
# staged config change (e.g. tightening racket_velocity_std) WITHOUT throwing away progress:
#   checkpoint_path=logs/rsl_rl/agibot_a3_hope/<run>/model_2000.pt
checkpoint_path: null
# With checkpoint_tolerant=true, checkpoint_path warm-starts ACROSS critic-layout changes
# (actor loads by name; mismatched critic re-initializes — deliberate warm-start semantics).
# Plain resume stays strict.
checkpoint_tolerant: false
# HitterPure geometry/face/clip/action contracts are compared against
# <checkpoint-run>/params/training_contract.json before loading. Historical checkpoints predate the
# sidecar; opting in here is a deliberate, logged representation transfer—not an ordinary resume.
checkpoint_allow_missing_contract: false
checkpoint_allow_contract_mismatch: false
# Formal task-first runs must pin the canonical receipt computed from the fully
# composed post-override Reward cfg.  Leave null for legacy/non-task-first runs.
# A task-first preflight with null fails before scene import and prints the
# candidate SHA to review; the real launch must pass that exact lowercase hash.
expected_effective_reward_recipe_sha256: null
# Fresh ActionBall policies normally keep the historical random actor
# initialization.  The N=1/N=5 diagnostic launcher opts into a reviewed
# shared-ready last-layer bootstrap explicitly; arbitrary-N banks stay on the
# historical path until they have an action-conditioned initializer.
action_ball_shared_ready_bootstrap: false
# Exact N=1 A3 dynamic-ready bootstrap.  The candidate and its independent
# Isaac nominal-hold PASS receipt are both byte-pinned before scene import;
# enabling the switch without all four pins fails closed.
action_ball_dynamic_ready_bootstrap: false
action_ball_dynamic_ready_artifact_path: null
action_ball_dynamic_ready_artifact_sha256: null
action_ball_dynamic_ready_nominal_receipt_path: null
action_ball_dynamic_ready_nominal_receipt_sha256: null
# Materialization-only escape hatch for producing the exact bootstrap-bound
# PPO recipe before a no-clobber launch.  It requires the explicit bootstrap,
# one environment, diagnostic ActionBall, and an unused absolute output path;
# the process closes the environment without constructing a PPO runner.
action_ball_policy_recipe_output_path: null

# logging
logger: wandb           # wandb | tensorboard | neptune
log_project_name: hope_wbc

# video capture (off by default)
video: false
video_length: 200
video_interval: 2000

# keep the working directory at the launch dir so logs/ lands next to the legacy script's logs
hydra:
  job:
    chdir: false
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
