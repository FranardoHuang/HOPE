# TIMELINE — 团队进展流水账(开会对进度用,长期更新)

**这是什么**:按时间记录"谁、什么时候、做了什么、解决了什么问题、依据是什么、效果如何、有没有证据"。
开会照着这份走。[NOW.md](NOW.md) 是"现在正在干什么"的看板,这里是"已经干成/干败了什么"的台账;
细节历史在 [PROGRESS.md](PROGRESS.md) 和各 gate 文档。

**写条目的规则**(所有人遵守):

1. 每完成或裁决一件事,当天往最新一周的"逐日流水"加一条,格式:
   `【谁】做了什么(人话)——解决什么问题;依据/参考;效果;证据(测试/数字/真机);状态`。
2. **效果必须带证据**:单元测试过了?对照实验数字?真机验证?都没有就老实写"未验证"。
3. **动奖励/训练开关之前,先看下面的台账**;动完当天把自己那行更新了。这是多人同时做奖励实验
   不打架的唯一机制。
4. 不用黑话。专业词第一次出现给一句人话解释(现成的对照表在 [NOW.md](NOW.md) 的"术语人话表")。
5. claude(franco 的 agent)的工程提交记在 franco 的 git 账号下;台账里按实际执行者写 claude,
   franco 本人的拍板/数据采集单独记 franco。

---

## 奖励与训练开关台账(累积,常驻;谁要动奖励先看这里再认领)

"开关"= 训练配置里可开可关的功能。状态:✅ 已进产品配方 / 🔬 实验中 / ❌ 已拒绝 / ⏸ 已并入代码但没启用。

| 开关(人话) | 谁做的 | 日期 | 想解决什么 | 状态/裁决 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 虚拟球奖励(球打到对面落点给分、压出球旋转给分;不用真的仿真球,击球瞬间用物理公式推落点) | yikang | 07-03 | 只模仿挥拍动作学不会"打到哪";要有打球效果的分数 | ✅ 产品线地基 | P2 产品线整条建立在它上面,击球三合格率 0.89;三个零梯度死锁修复(过网分数被卡死等)都已并入 |
| 消旋变体(vb_spin_mode=minimize:先不追求制造旋转,奖励"打到落点+出球越不转越好") | claude(franco 定语义) | 07-04 | 第一阶段先求稳定回球,旋转以后再说 | ✅ 产品配方 v2 | 对照实验:消旋组合 0.858/0.884 赢过老基线 0.813(NOW 07-04 10:00 判决) |
| 自适应模仿松紧(SMASH 论文做法:学得越好,模仿参考动作的容差自动收紧) | claude | 07-03 | 固定容差要么练不动要么练到顶 | ✅ 产品配方 v2 | 2000 步对照:+48% 提升(当轮最大单项收益) |
| 挥拍收尾起手(Ace 论文做法:一部分回合从"上一拍刚挥完"的姿态开始练,不总是从站姿开始) | claude | 07-03 | 真机是连续对打,不能每球都回到完美站姿 | ✅ 产品配方 v2 | 并入 P2 组合训练;部署仿真考试连挥 115 次不摔(07-05) |
| 动捕毛病仿真 A1(给策略"眼睛"加真实测量噪声与延迟,数值按场馆动捕实测拟合) | claude | 07-03~04 | 真机的目标信息有噪声/延迟,干净训练=真机踩坑 | ✅ 产品配方 v2 | 机制验证过;并入 P2 组合后整线达 0.89 |
| 感知三毛病 A1v2/R8(丢帧拿上一帧凑、击球后 0.03 秒看不见球、每拍一个固定小偏移) | claude | 07-04 | 场馆动捕在击球时刻的三种真实缺陷 | ✅ 07-05 新采纳 | 对照臂 0.890 ≈ 无代价白送抗噪 → 并入配方 |
| 等球奖励 hold_ready + 目标重放 HER(把自己打到过的击球状态混进新目标,目标永远"确实做得到") | jiayi | 07-02~03 并入 | 挥拍间隙要稳稳等球;目标太难导致学不动 | ⏸ 已并入 main;是否算进当前产品配方**待对齐**(本周开会定) | simtoreal2 谱系(model_9000);07-04 R14 已兼容 HER 的重放缩放 |
| 中途换招练习 clip_switch(挥到一半强制换一套动作重打,模拟规划器改主意) | claude | 07-04 | 场馆真机在换招瞬间摔倒 | ❌ 剂量 0.002 已拒绝(命中率税 0.12);低剂量 R11b 在跑;**07-05 新证据:专门的换招压力测试里,没练过换招的 P2 也 0 摔——这个开关买的东西目前量不出来** | R11 平台 0.762 vs 基线 0.89;压力测试 14 组全 0 摔(见 07-05 流水) |
| 减速入位 base_decel(PACE 论文思路:快到击球点时奖励脚下先减速站稳) | claude | 07-04 | 跑动接球最后一步刹不住 | 🔬 排队中(R12,看门狗自动点火) | 机制验证过,效果待信号档 |
| 变速播放 R14(同一套模板随机 0.8-1.2 倍速放,要求拍速同步缩放) | claude | 07-04 | 没有新数据的前提下练"要多快给多快" | 🔬 在跑(3000/4000) | 机制验证过(采样签名正确);对照裁决待跑完 |
| 手腕放开 R16(拍子那节手腕不再照抄视频朝向,只照抄挥拍路径;拍面由打球奖励自己教) | claude(franco 最高优先) | 07-05 | 视频测手腕朝向不可靠,照抄它给拍面质量封顶 | 🔬 在跑(3000/4000)。**07-05 定量佐证:整条 hopex 模板没有任何一帧胶皮面朝向对手**(见流水) | 机制验证过;裁决看真球考试回球率 |
| v5 新模板 R15(franco 07-04 拍的短条正反手视频当模仿模板) | franco 拍摄 / claude 接线 | 07-04~05 | 现役模板太长、首尾不贴准备姿态 | 🔬 排队中(等卡) | 击球相位已人工核对纠错(0.768→0.673);参考噪声比现役大 2-6 倍,判读时要当混杂因子 |

**多人同项对齐点**:奖励相关实验统一走 NOW.md 的"梯子"制度(先 3 分钟机制检查 → 2000-4000 步信号档
→ 赢家才长跑),同种子同热启,**每批必带同批对照臂**;裁决口径 = Isaac 击球三合格率 + 真球考试回球率双轴。

---

## 评估口径速查(裁决用哪把尺子;都在 `scripts/mujoco_eval_onnx.py` / `scoreboard_eval.py`)

| 尺子 | 量什么 | 什么时候用 |
| --- | --- | --- |
| Isaac 击球三合格率(composite) | 训练内:击球帧位置/速度/拍面朝向三项同时达标率 | 训练中看曲线、信号档裁决 |
| 记分板(4 协议 × 2400 步) | MuJoCo 里换一套物理引擎复考:Isaac 对照口径 / 门禁口径(explicit clipped-PD,"这里摔=真机摔")/ 部署仿真 ×2 | 存档要上真机前 |
| 真球考试 eval-B(--target-source venue-balls) | 用场馆实测来球分布出题,虚拟回球看合法落台率;**自带反事实列**(换上应有拍面朝向再评一次,量"拍面朝向单项损失多少") | 评"真实世界能不能打回去" |
| 固定拍面反解(--venue-fixed-normal) | 规划器迁就策略(拍面钉死只解拍速)能打回多少 = 不重训练的上限 | 评"改规划器 vs 改训练"的取舍 |
| 换招压力测试(--switch-stress p) | 挥拍中途强制换招,量摔倒率/换招后 2 秒存活/换招后命中率 | 评抗打断能力(R11 系列的尺子) |
| 部署仿真考试(--deploy-faithful) | 完全按真机流程连续挥拍(站姿起步、等球、挥完歇、不传送),主要看摔不摔、连挥多少次 | 上真机前的耐久检查 |

---

## 一周速览(2026-06-28 ~ 2026-07-05)

**jiayi —— 真机部署链从 0 到 1,踩掉四个部署级大坑**
把策略从训练环境搬上真机的整条链路建起来(06-30 首次部署),路上修掉四个都会导致真机事故的 bug:
模型打包时部分关节刚度/阻尼被写成 0(关节直接瘫)、真机端参考时钟用错(按错误帧发动作)、
下蹲保护阈值太严(正常下蹲被当危险动作强制罚站)、开机 IMU 偏航漂移(加了对齐检测)。
另外做了统一挥拍训练+HITTER 对齐、新观测设计、wandb 素材源修复。
⚠ **对齐项:07-03 的 5 个提交(规划器+MuJoCo 联调、ROS 话题标准化、完整运行命令文档)还在
simtoreal2 分支上没并进 main,开会要过一下。**

**yikang —— 球物理拟合 v1 + 虚拟球奖励地基 + 救活训练集群**
用场馆实测球轨迹拟合出这台"球的物理"(飞行阻力/马格努斯/弹跳/拍面接触,含验伪工具与报告),
在此之上写了虚拟球奖励(落点/过网/旋转)并修掉三个"分数永远是零"的死锁——这是 P2 产品线的地基。
07-03 把训练集群从"判死刑"救回来:根因是 Isaac 一个插件在无界面模式也要建 UI 卡死,打了环境变量
补丁绕过(全队 env.sh 都要带)。另外统一了"挥拍间不传送"的训练语义、修了配置层静默丢参数的坑。

**franco —— 数据、方向、三次关键纠错**
场馆采集来球轨迹数据 + 实战斜拍视频 + 07-04 v5 短条正反手模板;定下"先消旋求稳、旋转以后再说"
的第一阶段语义;现场观察验证了物理模型的预测(上旋回球不飞、侧旋飞出台 → 侧旋是补偿优先项);
三次纠错:抓出 P2.4 漏账集群、v5 击球相位标错(拍速峰=触球后甩鞭,不是触球)、废除专拍准备姿势
视频的计划(v5 首帧即可当锚点)。定 R16(手腕放开)为最高优先——本周末被定量证据坐实(见 07-05)。

**claude —— 评估基建、实验流水线、两个结构级发现**
修掉评估器 P0 大坑(所有模型评出全零的根因:模型打包链没带观测归一化 + 回合协议按旧谱系写死);
建 Isaac-free 模型打包链(集群挂了也能出模型)、固定协议记分板、算力手册(4096 环境×每卡最多 2 任务
定版)、消融梯子制度与看门狗;实现并机制验证 8 个训练开关(见台账);做真球考试 eval-B 并发现
**结构级问题:策略拍面朝向是模板锁死的,观测里没有"这球要什么拍面"的通道**(0% 回球,换上应有
拍面 100%);07-05 把"规划器迁就"路线做出来并判死(物理不可行),把"换招压力测试"尺子建好并量完
(结论:换招本身压不倒策略,R11 训练买不到东西)。

---

## 逐日流水(2026-06-28 ~ 2026-07-05)

### 06-28 ~ 07-01(周末前半 + 周一)

- 【jiayi】31 关节策略 ↔ 官方 29 关节仿真器的桥接脚本;模型打包同时带正反手两条模板;评估器加
  拍速记录与训练保护松绑开关——为第一次上真机铺路。参考:官方 AGI 仿真器接口。状态:并入 main。
- 【jiayi】**06-30 首次真机部署跑通**;07-01 统一挥拍训练 + HITTER 论文对齐 + 新观测设计并入 main。
  效果:真机能执行策略动作;后续所有部署工作的起点。
- 【claude】视频→动作模板的处理流程改成本地优先(不依赖外网服务),franco 的视频当天可转模板。

### 07-02(周三)——部署链修大坑日

- 【jiayi】**四个部署级 bug 一次修掉**:①模型打包可能把部分关节刚度/阻尼写成 0(该关节直接瘫);
  ②真机端参考时钟按旧一帧发动作;③下蹲保护阈值太严把正常动作当危险(强制罚站);④开机 IMU
  偏航漂移加对齐检测。证据:修复后真机链路可用,并入 main。**这四条是后来所有真机实验的前提。**
- 【yikang】训练语义修正:关闭"回合中途把机器人瞬移到模板某帧"的老做法(与真机不符),修目标
  重采样时进度分数的尖峰。注:这个功能和 jiayi 各写了一版,合并时费了劲——由此立了 NOW.md 的
  "先认领再动工"规则。
- 【yikang】修 HOPEPingPong 任务重置崩溃 + 素材注册表哨兵;验证 mp4→训练 全流程可跑。

### 07-03(周四)——评估基建日 + 集群复活日

- 【yikang】**训练集群救活**:Isaac 起不来的根因是 URDF 导入插件无界面模式也要建 UI、在这台机器
  上永不返回;打环境变量补丁跳过(HOPE_URDF_IMPORTER_NO_UI=1,全队 env.sh 要带)。效果:当天恢复
  训练;渲染/相机类任务仍不可用(已知限制)。
- 【yikang】**球物理拟合 v1 发布**:用场馆实测轨迹拟合飞行+弹跳+拍面接触参数(ball_physics_venue.yaml
  + 拟合报告 F1-F8 判定 + 验伪工具);修弹跳时刻 bug;规划器物理参数与拟合值对齐(3.4g 涂层球)。
  证据:报告在 docs/,拟合与验证代码在仓库,后被 07-04 现场观察验证过一条预测。
- 【yikang】**虚拟球奖励 Tier-1**(落点/过网/旋转,用拟合的接触模型+粗积分推落点)+ 修"过网分数
  零梯度死锁"。效果:P2 产品线的奖励地基。
- 【claude】**记分板**:固定 4 协议的 MuJoCo 复考工具(Isaac 对照 / 门禁口径 / 部署仿真×2),
  评估目标框改成按训练配置自动对齐(考卷和课本一致)。效果:此后所有存档用同一把尺子。
- 【claude】SMASH 式自适应模仿松紧 + Ace 式挥拍收尾起手实现并验证;2000 步对照:**自适应松紧
  +48%**,当轮最大单项收益。参考:SMASH / Ace 论文(docs/papers)。
- 【claude】A1 目标延迟/噪声训练开关(数值待动捕拟合);算力测定与定版(4096 环境最优;每卡最多
  2 任务、错峰 ≥60 秒;手册进 NOW.md);NOW.md 工作板制度上线(先认领再动工、板只在 main)。
- 【jiayi】素材源修复(wandb 只取最新模板);超参调整;规划器部分的训练重构。
  ⚠ 同日在 simtoreal2 分支上的规划器+MuJoCo 联调、ROS 话题标准化、完整运行命令文档 **至今未并入
  main(5 个提交)**——需要开会对齐怎么合。

### 07-04(周五)——真球分布进场 + 两个结构级动作

- 【claude】**评估器 P0 修复**:查明"所有模型评出全零"的两个根因——①模型打包链从来没带观测
  归一化(训练时策略吃的是归一化观测,评估喂生数据=喂乱码);②评估回合协议按旧谱系(每挥必
  瞬移)写死,对新一代连挥模型无效。修法:归一化边车文件+自动加载,回合协议按代际可选。
  **部署警示:真机 C++ 端同样裸喂观测,必须烤归一化进模型或在真机端归一化**——同日加了
  --bake-obs-norm 打包选项(归一化直接进模型文件)。证据:修复后 07-02 真机验证过的模型在评估器
  里分数恢复正常。
- 【claude】**场馆来球分布拟合进配置**(incoming_ball_venue.yaml):关键数字——手设训练来球范围
  只覆盖真实来球的 **20%**;训练框按"班级包络"重定(R7 臂在考)。
- 【claude】**真球考试 eval-B 上线并发现结构级问题**:策略对真实来球的位置/速度跟踪意外地好
  (3.7cm/0.18m/s),但**拍面朝向错 36-76°、回球率 0%;把拍面朝向换成应有值(其他不动)→ 100% 回球**。
  根因:观测契约里没有"这球需要什么拍面"的通道,策略只会摆模板里的拍面。两条路(规划器迁就 /
  改契约加通道)摆上台面。参考:docs/motion_and_contract_v3.md(逐条对照代码核实)。
- 【claude】StrikeSpec 反解规划器(给定来球和落点,反解应有拍面+拍速,并给出"控制精度预算":
  落点对拍速最敏感 0.85m 每 m/s);12 状态球估计器(影子模式)+ 旋转耦合弹跳(15 个新测试)。
- 【claude】训练开关三连:中途换招 clip_switch(R11,当天点火)、PACE 减速 base_decel(R12)、
  变速播放 R14(对抗性三视角复审通过,默认路径逐字节不变)。
- 【franco】现场观察验证物理模型预测:上旋回球不飞(接触抬升与飞行下压近似抵消)、侧旋飞出台
  (无抵消)→ **侧旋是旋转补偿的优先通道**。v5 短条正反手视频拍摄完成。
- 【franco】抓出 P2.4 漏账集群(准备动作定义、减速命令、拼接、位置命令回归四项没进账);当晚全部
  入账并排期。
- 【yikang】修训练配置层静默丢参数的坑(racket 系列键被丢弃导致覆盖不生效)。

### 07-05(周六)——三把新尺子 + 一个死刑判决

- 【franco→claude】v5 击球相位纠错:检测器选的"拍速峰"是触球后的甩鞭段,真触球在约 2/3 处
  (0.768→0.673)。**教训入库:拍速峰≠触球,选帧要用前向速度峰+人工先验交叉验证。** R15 训练框
  按对相位重算。
- 【claude】R16 手腕放开实现+机制验证(franco 最高优先);o3 轮点火(R16/R14/R11b);GPU 撞车
  复盘(3 任务挤一卡)→ 发射前查卡制度落地(看门狗 wait_slot);R11 在平台 0.762 停跑(命中率
  税 0.12),"不跑全程"制度化;NOW.md 术语人话表 + 胜利组合常驻区上线。
- 【claude】**真球考试反事实固化**:每次击球自动多评一次"换上应有拍面"(cf 列),不再是手工分析。
  正式记录:实际 0/44,反事实 44/44(落点中位差 0.10m);当日 2400 步存档逐字节复现。
- 【claude】**"规划器迁就"路线判死刑**:固定拍面反解实现(16/16 测试)后复测 = 0%,且暴力扫描
  证明是物理不可行——**正手拍面(几乎朝正侧方)任何 ≤6m/s 拍速连网都过不了;反手只剩贴网大斜线
  小缝(在合法落点框外)**。进一步甄别:**整条 hopex 模板没有任何一帧胶皮面朝向对手**(机器人模型
  已核实胶皮面=腕系 +Y,0.0° 偏差)→ 不是相位标错,是视频重定向的手腕朝向整体不可信。
  **结论:改契约加拍面通道(175→179)从可选升级为必须;R16/新模板是执行机构。** 需要 franco/全队
  排期"契约日"。
- 【claude】**换招压力测试尺子建成并量完**:{P2 没练过换招, R11 练过, R11b 低剂量临时} × {Isaac
  对照, 门禁口径} × 三档换招率,24000 步/组,共 14 组——**全部 0 摔、换招后 2 秒存活率 100%、
  换招后命中率≈干净挥拍**。判读:换招瞬间的参考跳变本身压不倒策略,R11 训练买的东西在仿真里
  量不出来,而它的命中率税是真的 → R11@0.002 维持拒绝;真机换招摔倒更可能是"换招×感知毛病×延迟"
  的交互,要追就叠加噪声再考,不是继续扫剂量。
- 【claude】**连挥耐久测试(franco 问"最好的模型能连挥几次不倒")**:P2 产品线按真机流程
  (站姿起步、等球、挥完歇 1.5 秒、不传送)连续挥拍——正手 113/113、反手 117/117、正反交替
  115/115,门禁口径与 Isaac 对照口径**全部 0 摔**;瓶颈是测试时长(8 分钟仿真),不是机器人。

---

## 下周要对齐/拍板的事(2026-07-06 周会用)

1. **契约日排期(franco + 全队)**:观测 175→179(加拍面指令 3 维 + 强度 1 维)。路 A 判死后这是
   唯一活路;R16 若赢,正好是它的执行机构。涉及:训练奖励参照改指令、真机 C++ 观测端同步。
2. **jiayi simtoreal2 分支 5 个提交怎么并**(规划器+MuJoCo 联调、ROS 话题标准化、运行命令文档)。
3. **hold_ready / HER 是否算进产品配方**(现在的台账状态是"已并入代码、配方归属未定")。
4. **o3 轮裁决**(R14 变速 / R16 手腕放开 / R11b 低剂量换招,都在 3000/4000):按梯子制度出信号档
   结论;R11b 若税仍在,换招方向整体关闭。
5. **产品线代表存档重新导出**:胜利组合区说"选峰值(0.893@11-12k)别拿最后一个(13.5k 回落
   0.883)",但现役评估用的打包模型出处迭代未记录——确认一下,若是 13.5k 就按峰值重导。
6. R11b 跑完后的例行补量(换招压力测试终值,一条命令,claude 已挂自动监视)。
