# TIMELINE — main 上值得记住的重要变化

Git 已经保存每个 commit 及其注释，因此本文件不复述 commit 流水。这里只记录以后团队必须知道的
重要根因修复、合同变化、方向翻案和共享能力。

规则：

1. 只有已经进入 `main` 的变化才能写；off-main WIP、排队状态和实验运行本身不写。若 main 的重要
   决策就是因明确缺口拒绝候选，可记录 `NO-MERGE` 原因，但不能把候选能力写成已合入。
2. 一个逻辑成果只写一行，即使红队过程中修了多个 commit。
3. 必须说清楚“原来错在哪、修了什么、还缺什么”；测试数量本身不是成果。
4. `人类责任人`只能写人；Claude/Codex 只能写在`执行者`。
5. 详细结果去[实验目录](experiments/README.md)，当前采用状态去 [NOW](NOW.md)，验收去 Gate 文档。

旧逐日流水完整保存在[历史 TIMELINE](experiments/archive/TIMELINE_legacy_through_2026-07-12.md)。
本文缩写的现行人话释义见 [术语与人话对照](DEFINITIONS.md)。

## 当前重要变化

| 日期 | 重要变化 | 人类责任人 | 执行者 | 修复/澄清了什么 | main 证据 | 仍然缺什么 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-07-17 | 动作加速的一次性筛选改为复用候选并闭合 MJCF 外部资产 | Franco | Codex | Stage-2 v1 曾混淆 float32/float64 速度 producer；v2 修好后又只复制 XML、漏掉其引用网格，导致四格在 TOPP 算法前失败。后续不再重跑 generator，逐字节复验并复用 v2 四份 candidate/contract，把唯一变量缩到 MJCF closure；从冻结 Git objects 绑定 `1 XML + 74 mesh`，拒绝 include、路径逃逸、错误 tree/blob/mode、symlink 与 worktree 漂移。v3 dry-run 又在结果 root 前抓到 v1/v2 contract SHA 混账，v4 改为绑定实际 v2 contract。 | [0.5 秒动作卷宗](experiments/2026-07/EXP-MOTION-READY-TO-STRIKE-0P5.md)、[运行入口](operations/run_ready_to_strike_motion.md#stage-2-四个中点的一次性执行当前-v4)、[G08](gates/G08_blind_spot_improvements.md) | 这只关闭 source/lineage 假失败；v4 远端 TOPP、`<=0.5 s`、L0/L1、桌网、动力学、K100、vendor MuJoCo 与真机仍未通过。 |
| 2026-07-16 | 部署与训练改为“同一来球、递增 revision、挥拍中实时更新” | Franco | Codex | 旧 formal 179-D runner 为保护一次性命令而在 engage 后冻结 target/TTS，训练也无法表达同球修订，导致 stale intercept 与同球重复开新 task。现用 `(control_epoch, task_id, task_revision)` 区分新球和同球新估计；触球前每个 policy tick 原子更新位置/速度/signed face/TTS，phase governor 只接受动力学可达的提前截止，VRPN capture age 进入补偿，actor 看修订而题库/Reward truth 不被改写。训练准备时间显式覆盖 `<0.5` stress、exact `0.5`、`0.5–0.9` 与 `0.9–1.7 s`，并逐 update 写整数行为账。4096-env A6 已实测所有机制；同父本淘汰 consumer 现以两份整数窗和 portfolio receipt 执行 `+200/+500/+1000`，至少保留两条、实际 exact-0.5 暴露与 broad 时间覆盖。 | [task-revision 卷宗](experiments/2026-07/EXP-P1-TASK-REVISION-CUTOVER.md)、[policy 接口](interfaces/policy_observation_action.md)、[队列操作](operations/run_lean_training_queue.md) | 19 条当前 live、3 条是启动基础设施拒绝；首次 `+200` scan 因 still-live sibling 尚未共同到档而合法零 stop。尚无 0.5 秒 K100 行为分数、vendor MuJoCo 或真机结果；v4rg TOPP 仅有 `0.98/0.78 s` 可行上界，positive-delay transport 仍 NO-LAUNCH。 |
| 2026-07-16 | rolling checkpoint 可精确取证，但现役 EMA 量尺不能自动淘汰 | Franco | Codex | 旧计划把两个 100-update 行为窗写进淘汰规则，却没有先证明 logger 能物化它们；现役 completion/fall 是重叠历史 EMA，physical-fall union、ready-phase 分母/累计和与母本机器基线均缺失。只读 inspector 证明首份 `model_5200` 绑定的是 corrected-budget 的较早 reviewed runner，不是坏 claim；attestor 现由独立 contract 对每个 job 完整重建并精确白名单两个 reviewed claim variant，actual digest 进入 runtime 二次核验，旧 budget 或任意其他漂移仍拒绝。两个不同 job 的 historical-claim no-clobber receipt 均已实测 embedded/finite/hard/claim/lineage 全过；完整 claim digest 必须逐 job 重建，不能把一个 job 的摘要当全局 variant ID。行为字段不全时仍必须写“量尺不完整，继续训练”。 | [rolling 卷宗](experiments/2026-07/EXP-P1-ROLLING-TIMING-SUPERCOMBO.md)、[操作](operations/run_lean_training_queue.md#rolling-timing-双-pod-严格续训2026-07-16)、[G05](gates/G05_isaac_training_first_loop.md) | 当前 22 条不能倒灌整数行为账，也不能据旧 EMA stop；未来 source 需逐 update 整数事件与 phase `sum+count`，或另立 checkpoint-bound immutable exam。 |
| 2026-07-16 | Isaac 球空气动力作用点从错误的“默认 COM”说明改为 link-origin + zero-offset 门 | Franco | Codex | 固定 Isaac Lab 2.1 实际把 `position_data=None` 的 BODY wrench 施加在 link transform origin；旧注释称 COM。当前三类原点居中球的 COM offset 为零，所以行为不变；standalone in-loop 检查现要求该 offset 逐 bit 为零，未来换偏置资产会 fail closed。 | [G04](gates/G04_sim_modeling_mujoco_isaac.md)、`isaac_ball_inloop_check.py` | 还需在 Pod Isaac runtime 重跑 in-loop 卷；该修复不证明随机躯干推力已进入 solver。 |
| 2026-07-16 | 双 Pod 点火只跨主机并发，同主机仍保持 boot 串行 | Franco | Codex | 逐条等待 4096-env 首迭代会把两台互不依赖的 Pod 无意义串成近两小时；全并发又会放大同主机 importer 风险。fill 现每批最多 Pod1/Pod2 各一条，同 Pod 永不并发并继续受 host boot lock 保护；部分失败先等 sibling settle、保留成功证据，再停批且不重放。 | [rolling queue 操作](operations/run_lean_training_queue.md#rolling-timing-双-pod-严格续训2026-07-16) | 只减少点火墙钟时间；动态 importer malloc 仍可能使单槽失败，行为曲线尚未形成。 |
| 2026-07-16 | RSL 续训 budget 明确拆成 trainer 追加步数与绝对 checkpoint 边界 | Franco | Codex | 首条真实 continuation 证明，parent `1600` 时把绝对 `3601` 传给 `max_iterations` 会得到 `1601/5201`，因为 RSL 将其当恢复后的追加步数。runner 现给 trainer 传 `2001`，claim 同时绑定追加值、absolute exclusive bound=`3601` 与最后 checkpoint=`3600`，marker/里程碑继续用绝对编号。 | [rolling queue 操作](operations/run_lean_training_queue.md#rolling-timing-双-pod-严格续训2026-07-16)、[组合卷宗](experiments/2026-07/EXP-P1-ROLLING-TIMING-SUPERCOMBO.md#点火首条抓到的-resume-budget-语义反例) | 首条 v1 schedule 只能作 inexact 方向诊断并须在 `model_3600` 精确收口；当前只到 `model_3300`，且停臂 consumer 尚未闭合。修正后 accepted children 已越过首迭代，仍缺可比较行为量尺。 |
| 2026-07-16 | 多父本组合续训从手工路径猜测升级为 source-bound continuation queue | Franco | Codex | generic queue 正确拒绝续训，但临时绕过会丢 optimizer、把相对 milestone 当绝对编号，或让 Pod/parent/YAML 状态漂移。专用 runner 现绑定同 Pod checkpoint/hard/claim/binding、actor+critic/optimizer、最终 Hydra argv、自身 bytes 和四轮六卡分配；激活只接受明确状态与逐字段 full-scene 证据，timeout/失败不自动重放。 | [G05 rolling source 门](gates/G05_isaac_training_first_loop.md#2026-07-16-rolling-targettts-与题库-retiming-源码门)、[rolling queue 操作](operations/run_lean_training_queue.md#rolling-timing-双-pod-严格续训2026-07-16) | 三份 parent runtime inspect 已过；24 个唯一 claim 均已消费，22 条 accepted 已越过首迭代并持续 checkpoint-finite，2 条 importer rc134 保留为基础设施拒绝。仍缺行为量尺与 vendor MuJoCo。 |
| 2026-07-16 | full-scene supervisor 不再把 Popen 后单次 `/proc` 竞态固化成空身份 | Franco | Codex | 4096-env rolling probe 的训练本体自然成功，但 supervisor 在 child 刚 Popen 后只读一次 `/proc`，瞬时失败便永久写 `starttime=null`，使正确 binding 被 strict finalizer 拒绝。现用有界只读重采样，只有 PID/PGID/正 starttime/post-exec argv 全匹配才接受；child 早退或超时仍明确 fail closed，且不 signal。 | [G05 rolling source 门](gates/G05_isaac_training_first_loop.md#2026-07-16-rolling-targettts-与题库-retiming-源码门)、[队列操作](operations/run_lean_training_queue.md#正式环境数先做-full-scene-probe) | 旧 a1 failure 保留且不重跑；修复后的 strict finalizer 尚无新 Pod receipt，不倒算旧结果。 |
| 2026-07-16 | 训练目标把剩余击球时间纳入同源延迟元组，题库与老师动作加速不再假冲突 | Franco | Codex | 旧 A1 只原子延迟位置/速度/拍面/侧别，TTS 仍取 live clock，会形成 mixed-age planner observation；旧启动门又把任何 question-bank+retiming 一概拒绝。现显式 compensated 模式延迟完整元组并扣除已知 step delay，critic/Reward 保留 live truth；retiming 只改老师路径时间，最终 bank 绝对物理答案保持不变。 | `704bf3a2`、[policy 接口](interfaces/policy_observation_action.md#actor-visible-planner-tuple-timing)、[rolling 组合卷宗](experiments/2026-07/EXP-P1-ROLLING-TIMING-SUPERCOMBO.md) | 只过 Pod2 增量 source gate；4096-env probe、0.5 秒行为、部署 capture timestamp/rolling revision 与 vendor MuJoCo 均未通过。 |
| 2026-07-16 | 演示续训从可变 live checkpoint 改为一次性只读母本快照 | Franco | Codex | 旧续训草案既未冻结完整组合配方，也会在“先验 checkpoint、后启动 trainer”之间留下可替换窗口，并可能只凭恢复日志就误报首迭代。专用队列现把完整配方、父 claim/binding、source/host/slot 全部冻结；一次性复制四件套只读快照，启动锁内再次核 SHA，并要求绑定 PID/PGID/starttime/cmdline 仍存活且确实出现恢复后的训练迭代。基础设施 retry 又在同一锁内重核旧失败证据、旧 PGID 全成员与 NVML 残留，且只允许一次新 namespace。所有后代永久标为非正式证据。 | `7882f9df`、`af884ad4`、`6d54671f`、receipt `fd200bd6...f2f34`、[演示组合卷宗](experiments/2026-07/EXP-P1-DEMO-HOTSTART-PORTFOLIO.md)、[队列操作](operations/run_lean_training_queue.md#demo-only-model-3500-严格续训2026-07-16) | 七条后代均已越过首迭代，其中两条 `model_3700` 的 finite/lineage/contract 通过；仍缺 checkpoint 行为排序和 vendor MuJoCo。 |
| 2026-07-15 | 稀疏击球 Reward 早筛改为“机会与资格”整数账本 | Franco | Codex | 旧早筛把尚未形成触球机会的零值和机制失效混在一起，容易在稀疏 Reward 尚无资格时误杀训练。新账本按侧逐项记录 strike→capture→net→landing→legal-return，并把证据状态分成可继续、可比较和机制异常；只有总机会、每侧机会及连续里程碑都满足时才允许作输赢判断。 | `8c485dce`、[稀疏 Reward 资格接口](interfaces/sparse_reward_eligibility_ledger.md)、[实验卷宗](experiments/2026-07/EXP-P1-SPARSE-REWARD-ELIGIBILITY.md) | 当前在跑的 `2c2d70d` source 没有这些计数器，不能从旧 EMA 反推；Phase-B runtime adapter 与双引擎行为仍缺。 |
| 2026-07-15 | 连续等待冻结为“所选动作第 0 帧 + 全零速度”的原子合同 | Franco | Codex | 旧实现可能把默认站姿当 ready、保留 root/关节/body 速度，或每 tick 重锚 XY，无法表达“已经回到一个随时可接下一拍的真正静止准备态”。新机器合同绑定所选动作自身第 0 帧、全部速度为零、冻结 XY 与原子揭示，禁止 teleport、reset 和清历史。 | `8af9e885`、[T1 event 接口](interfaces/t1_event_training_contract.md)、[Recovery A/B/C 卷宗](experiments/2026-07/EXP-RECOVERY-TUPLE-ABC.md) | 现役训练源码仍违反该合同，完整 adapter 与 full-scene probe 未完成，因此保持 `NO-LAUNCH`，不混入今晚 demo 组合。 |
| 2026-07-15 | post-swing 外生 teacher 从自生 buffer 改成独立可追溯制品 | Franco | Codex | 旧配对让每个 policy 只有活到自然 clip wrap 才自己填恢复 buffer，base-decel treatment 因此会改变 curriculum ready 时刻，不能作主效应归因。main 现分开绑定原 capture producer、固定 attestor 与一次性 authorization；合法 SHA 重绑、dirty source、任意 pickle 和自签 clean HEAD 均 fail closed。Pod2 已自然采集并 attest 4096 条状态，merged-main status 对 receipt `e20a6989...d2aba4` 给出 exact binding。 | `a1b0b1b`、receipt `e20a6989...d2aba4`、[teacher artifact 合同](interfaces/post_swing_teacher_artifact.md)、[运行操作](operations/run_post_swing_teacher_capture.md)、[measurement 卷宗](experiments/2026-07/EXP-P1-V1V2-BASE-DECEL-MEASUREMENT-RERUN.md) | 仍缺 4096-env 首 reset 在 PPO rollout 前的采用率/readback probe；其通过前不能发科学 pair、第二 seed 或 judge。 |
| 2026-07-15 | Franco 反手拉 B 通过整轨桌网有限密扫门 | Franco | Codex | 旧 harness 先后把 MuJoCo world-geom 编号、joint-order 注释、reporting cap 下界与 5/20 mm 安全 epsilon 做错，产生假拒绝和一份下界多报的旧证书。main v4 将安全阈值与 reporting 数值搜索分离，并在 Pod2 唯一 audit 中得到 1201 帧 hard/warning/unsafe 全零、诚实 saturated lower `0.099999999999 m`。 | `c047ea7`、certificate `93fd5435...9b0e7`、[桌网卷宗](experiments/2026-07/EXP-MOTION-BACKHAND-LOOP-B-TABLE-NET.md)、[G08](gates/G08_blind_spot_improvements.md) | 这是 400 Hz 有限密扫，不是连续时间或动力学行为；B 只解锁 vendor 动力学/平衡门，RL/Gate3/真机仍 blocked。 |
| 2026-07-15 | Franco 反手拉 B 通过 vendor L1 整轨自碰/自打门 | Franco | Codex | L1 harness 先后把 private helper import 与 schema2 runtime/GMR joint order 做错，曾假报 module 缺失和左脚踝超限；main 现用 exact-path loader 与 31-name permutation fail closed。修后 1201 个有限 400 Hz 样本自碰/自打/warning 全零并发布唯一 certificate。 | `7dec698`、certificate `6840df34...db60`、[L1 卷宗](experiments/2026-07/EXP-MOTION-BACKHAND-LOOP-B-VENDOR-L1.md)、[G08](gates/G08_blind_spot_improvements.md) | 这不是连续时间、桌网或动力学证明；B 只解锁整轨桌网余隙，RL/Gate3/真机仍 blocked。 |
| 2026-07-16 | 随机横向躯干推力有了默认关闭、可取证的 trainer 接口 | Franco | Codex | 旧草案允许伪造 ACK、异步写后提前签收或让坏回执遗留非零力；后续又可能把 setter 提交误写成 solver 已执行。现 L0/L1 共同随机题、scheduler、显式 COM Isaac adapter、Hydra 入口、EventManager manifest、checkpoint hard contract 与有界标量账已接通，逐环境区分 sampled/commanded/backend-accepted/abandoned；默认关闭且不改变旧 hard contract。 | [扰动卷宗](experiments/2026-07/EXP-P1-LATERAL-BALANCE-PERTURBATION.md)、[adapter 合同](interfaces/lateral_perturbation_adapter_contract.md)、[G05](gates/G05_isaac_training_first_loop.md) | `launch_authorized=false`；仍缺 clean full-scene、独立 dynamics-response、no-host-sync/吞吐、terminal-zero 与 exact runtime receipt。backend-accepted 绝不等于 solver consumed。 |
| 2026-07-15 | 解析 outcome Reward 与物理裁判语义分开记账 | Franco | Codex | 旧 NOW 把 `vb_metrics_only=true` 误读成解析上台只记指标；实际 VirtualBall task 仍以 achieved 球拍状态支付解析过网/落点/旋转 `20/30/5`，且有合法回球前的部分分。真正 metrics-only 的 Phase-A engine-integrated ball 当前无拍球冲量，不提供物理回球 Reward。 | [Reward 真值审计](experiments/2026-07/EXP-P1-REWARD-PHYSICAL-TRUTH-AUDIT-20260715.md)、[G05](gates/G05_isaac_training_first_loop.md) | 先闭合 Phase-B receiver 与 vendor MuJoCo 对拍，再做 fixed-budget analytic/physical outcome mixture 单 seed 消融；现役训练不改合同。 |
| 2026-07-15 | Pod/GPU 资源归属进入 fail-closed 发射合同 | Franco | Codex | 旧 `preferred_slot` 满载会 fallback，非科学 warmup 与底层 claim 又可绕开槽选择，存在 Codex 作业误落一康 GPU0 的风险。现 `required_slot` 从 schema、调度、claim 到 warmup/probe/finalizer 全链硬绑定；满槽只等待本 job，不饿死其他 GPU。 | `8b0a084`、[队列操作](operations/run_lean_training_queue.md)、[G05](gates/G05_isaac_training_first_loop.md) | 字段只保证单 job 资源隔离，不提供 matched pair 原子性；replacement pair 仍须 fresh strict probe 后才能发射。 |
| 2026-07-14 | full-scene probe 终档从“有 model”升级为实际场景真值门 | Franco | Codex | 旧终档语义可在没有绑定实际环境数、物理球/桌实体或正式 schema-3 validator 时给出 unlock。现 pass 必须同时绑定 actual 4096 environments、物理球与三个 scene entity、face179、31/31 零摩擦、完整正式 schema-3、finite model 和自然空 PGID；短终态只能冻结 failure，不能假绿。 | `caeb9ad`、[G05](gates/G05_isaac_training_first_loop.md)、[队列操作](operations/run_lean_training_queue.md) | 该能力只验启动/终档基础设施；conditional 与 V1+V2×base-decel 科学配对尚无 Reward、checkpoint 早判或行为结果。 |
| 2026-07-14 | Pod2-only 发射热路径不再越界或重复预检 | Franco | Codex | full-scene probe 原先即使只 dispatch Pod2 仍会读取 reserved Pod1；`fill` 又在原子 launch 内外重复同一 doctor，增加一次无锁 SSH/Hydra 失败面。现 probe 只读 selected slot，fill 每臂只发一次内含完整 preflight 的原子 SSH。 | `aaf188b`、`7222e3f`、[队列操作](operations/run_lean_training_queue.md) | 两组 matched pair 均已过 first iteration，但仍无 milestone checkpoint 或 activation 早判。 |
| 2026-07-14 | 每 GPU launch lock 不再被长训继承 | Franco | Codex | `flock FILE command` 的私有 fd 被 detached trainer 继承，使“Pod2 每卡3条”实际退化为每卡只能发1条；现由短命 controller 显式持 fd8，launcher child `8>&-`，锁只覆盖 doctor/容量/claim/boot。`preferred_slot` 让已过 cold-boot 的配对优先同卡，满载才回 round-robin。 | `4d492a1`、[队列操作](operations/run_lean_training_queue.md) | 现役旧 trainer 已继承的锁只等自然退出；仍需真实第二条同卡 first-iteration 验证修复。 |
| 2026-07-14 | 冷场景导入与科学训练 namespace 分离 | Franco | Codex | 动态 A3 URDF import 会在 reward/contract/PPO 前偶发卡死；真实反例又证明 1-env warmup 通过仍可在 4096 env 卡死。1-env 现只作 cache probe；`full-scene-probe` 保留 exact source/GPU/动作/bank/plant 与正式环境数，只在独立 2-update 非科学 namespace 越过 first iteration，且 source-pinned stale watchdog 只收口自身 PGID。 | `56535d5`、`9a115fe`、`077e70c`、[队列操作](operations/run_lean_training_queue.md#冷启动urdf-import-诊断) | strict caeb 4096-env receipt 已通过；长期仍需 content-bound 预转换 USD，科学配对也仍未点火。 |
| 2026-07-14 | 固定预算拍面/就绪联合塑形进入训练源码 | Franco | Codex | 朴素 `readiness*face_error` 配负权重会奖励策略故意离开就绪门；新公式在击球窗内保留固定成本，只允许位置/拍速就绪与拍面正确共同减免，并以标量单调反例锁住“误差变差不能少罚”。默认关闭，门宽与公式进入 hard contract。 | `61007e9`、[实验卷宗](experiments/2026-07/EXP-P1-CONDITIONAL-FACE-GUIDANCE.md) | 仍缺同 source control/treatment 的 Pod runtime、activation 分母与 `+200/+500/+1000` 行为早判。 |
| 2026-07-14 | 探索训练从手拼命令升级为 trainer-owned 可取证 YAML 合同 | Franco | Codex | 旧入口允许重复/owned Hydra key、未解析配方和复用 run directory，外部又会按 timestamp/glob 猜 checkpoint。现 canonical claim 绑定真实 argv，trainer 自己绑定 actual source/process/GPU/RSL log dir；attestor 只沿不可变 binding 核 exact milestone。`dispatch_pods` 同时把“Pod1 留给 Yikang、Codex 只用 Pod2”做成机器合同。 | `169099c`、`97c341c`、`0b632c7`、[运行绑定接口](interfaces/lean_training_run_binding.md) | 新 source trainer 已越过 first iteration；仍缺首个 milestone receipt 与各机制 activation 分母，旧 run 不追认。 |
| 2026-07-14 | 零摩擦声明从配置文字升级为贯穿发射与 checkpoint 的可验证合同 | Franco | Codex | 旧 C2 只在 manifest 写零摩擦，argv/recipe 漏传，真实 31 项全为非零；C3/D3 现把同一事实绑定到唯一 argv、recipe、claim、runtime marker、hard contract 与 checkpoint replay，并在 Pod1 发布 paired L1 receipt。 | `c2e81ba`、[C3/D3 卷宗](experiments/2026-07/EXP-P1-SIGNED-FACE-C3D3-ZERO-FRICTION-L1.md) | 只闭合 provenance；同卷 K100、L2、第二 seed、MuJoCo/Gate3 行为仍未通过。 |
| 2026-07-14 | MuJoCo evaluator 的控制、自碰与 frame/provenance 语义进入同一严格合同 | Franco | Codex | 旧 evaluator 可把 P/D 分别截断而不是截总力矩、只在 control step 末看自碰、混用 pelvis link origin 与 COM/世界系 gyro，并让可覆写的 `partial` provenance 或旧 scoreboard 继续假绿；现 total-PD 后一次 clip、每个 physics substep 拒绝机器人自碰、COM/gyro frame 显式化，并让 mask/adapter/scoreboard 的来源与撤销 fail closed。 | `fef3eb4`、`c11551a`、`9aeb059`、[frame integration 卷宗](experiments/2026-07/EXP-MUJOCO-EVAL-FRAME-INTEGRATION.md) | Pod2 tiny MuJoCo optional source gate 已 `10/10`；仍无 vendor MJCF policy rollout、同 policy 双引擎归因或 Gate3 行为通过。 |
| 2026-07-13 | schema-3 题库可做严格加法式物理合同重绑定 | franco | Codex | 目标源码只新增 signed-face helper 时，旧 loader 会正确拒绝整文件 SHA；新 consumer 不走 legacy，而以 Git/AST、全部问题数组 raw bytes、精确四-leaf metadata 和全题 old/new bitwise physics replay 发布新制品。 | `ecab785`、`62dfbbf`、[signed-face 漏斗](experiments/2026-07/EXP-P1-SIGNED-FACE-RESCUE-FUNNEL.md) | v6 L1 尚未跑；新 train family 的 exam 也未重绑定，不能启动 exact judge/L2。 |
| 2026-07-13 | checkpoint 与 detached-worktree 发射闭包对齐实际运行布局 | franco | Codex | 首版误读 checkpoint；次版漏 source-first 环境；第三版过早 import Isaac；第四版又暴露 ignored A3 资产不随 worktree。现递归审计 `infos`，绑定确定性环境/模块 origin，并同时绑定 clean restore source 与 target 的完整 A3 tree；旧失败证据均保留。 | [signed-face 漏斗](experiments/2026-07/EXP-P1-SIGNED-FACE-RESCUE-FUNNEL.md) | 仅修发射闭包；L1 行为、directional paper、MuJoCo/Gate3 仍未通过。 |
| 2026-07-13 | signed-face 身份门进入训练与解析共用量尺 | franco | Codex | 旧 Isaac virtual reward 与 NumPy/MuJoCo analytic scorer 会在 `orient_normal` 后把 `n/-n` 当同一平面；现先绑定 raw-A、逐 clip `[+1,-1]` physical-B 与 +X/hemisphere，负控证明错面不再得分，旧 unsigned 只能显式 inexact。 | `fdc3964`、`f4a2f3a`、[拍面符号卷宗](experiments/2026-07/EXP-P1-FACE-SIGN-FORENSIC.md) | fresh 训练 canary、修正后同卷和厂商 MuJoCo 行为仍未跑。 |
| 2026-07-13 | 私有动作 GVHMR queue 改为 exact batch + 不可变 child 输入 | franco | Codex | 旧通用 launcher 可把可变 staging 路径直接交给 child，也会混淆 Franco/v12/static/motion 优先级；现 S0/M0 只接受各自 prereg 和一次性 record，child 只读 O_EXCL 私有快照，并绑定 source/runtime/output/audit，旧入口只留历史证据。spatial validator 同时修正了外参声明字段层级的假拒绝。 | `882fea4`、`a4bbbaa`、[GVHMR 小批](experiments/motion_video_gvhmr_prereg_franco_static_motion_20260713.md) | 结构结果不等于机器人动作；仍缺 GMR/schema-2、足接触、桌网、自碰、动力学和行为卷。 |
| 2026-07-13 | model-4000 同卷推翻 `SZ` 稳定候选并实证拍面符号盲区 | franco | Codex | 四 seed 4k 为 `50/88/98/0`，稳定门全失败，seed4 非晚熟；同时 seed2/3 parsed 正手高分与 signed composite `0/50` 并存，因此旧分不再能选 baseline。 | [稳定性卷宗](experiments/2026-07/EXP-P1-FRESH-SZ-STABILITY.md)、[aggregate](../configs/phase1_fresh_SZ_model4000_seed_stability_q50_aggregate_result_20260713.json) | signed 源码门已补；仍需 fresh canary、修正后同卷、厂商 Gate3/Gate3B 与 physical ball。 |
| 2026-07-13 | q50 一次性持久启动合同 | franco | Codex | 旧 activation consumer 的顶层进程会随 SSH shell 丢失，而 detached judge 仍可能继续；新 supervisor 只在核完进程身份与全 SHA 闭包后发布不可逆 token，token 可见后的任何异常都不得产生重试权。 | `2f2e705`、[执行子记录](experiments/phase1_fresh_sz_model4000_q50_20260713.md) | Linux `/proc` fake-runner、Pod 部署和真实 MuJoCo q50 尚未运行。 |
| 2026-07-13 | formal planner-policy causal tuple 与 exact portable Release 集成 | franco | Codex | 旧候选会让跨 topic 的旧 target/base 重新配对，或在读取本 tick base 前 engage；现用 shared epoch、command/base sequence、最新 tick-start base 与 revocation generation 统一 actor gate，并让 exact 源码通过 strict-finite Release。 | `6d6b778`、[实验卷宗](experiments/2026-07/EXP-GATE3-PLANNER-POLICY-RELEASE-BUILD.md) | ROS/Jazzy/AimRT、formal ONNX runtime、owned backend first tick、vendor MuJoCo 行为和硬件均未运行。 |
| 2026-07-13 | native MuJoCo feasibility/implementation 升为 P0，且不阻塞 `Gate3-D0` | franco | Codex | Isaac 高分与 MuJoCo 解析回球差反复背离，因此停止优先扩 Isaac-only sweep；首卷只做无球平衡/击球状态。红队又冻结 action trace/clamp、alias/exec、strict JSON、MJCF `strippath` 四项正确性缺口，以及 N=1/8/32/64、48 h 留 30% 余量的吞吐门。 | `abaea9b`、`68c0c2a`（`af803e9`）、`7c5f45b` | off-main preflight 候选仍为 `NO-MERGE`；无 trainer、backend、PPO smoke、physical return 或连续结果。 |
| 2026-07-13 | 文档总索引与实验登记骨架补回 main | franco | Codex | main 原本缺少统一 INDEX 和实验登记，任务路由分散在 Gate、operation 与聊天中；现补上一站式入口和实验最小字段。 | `3c7e507` | 该提交只有英文骨架，仍缺中文术语、完整实验卷宗和精简三本账。 |
| 2026-07-12 | strict 179-D face model contract | franco | Codex | 关闭两条假绿：把 actor raw face A 与对手向 physical face B 混淆；no-publish 顺便绕过模型校验。现在 export/runtime 绑定 per-clip sign、bank envelope 和 exact metadata。 | `8975043` | real-model preflight 通过；vendor behavior、self-hit、recovery 未测。 |
| 2026-07-12 | Gate3 source gate 与 runtime ownership 分层 | franco | Codex | legacy 脚本使用 broad process match，无法证明 fresh state。main 只接收 plan-only source gate 和明确 inexact 的 first-actor-candidate 诊断，进程启动/锁/信号所有权另立行为门。 | `b2067ba`、`7c0c385` | 无 owned vendor first tick 或 `Gate3-D0` behavior。 |
| 2026-07-13 | recovery A/B/C 结构合同与随机到球设计边界进入 main | franco | Codex | deploy idle 的混代 tuple 被定为 OOD，A bridge、B ready tuple、C previous tuple 分开；07-13 文档又将 T0/T1/T2、硬安全、随机到球环境轴和 reward 消融次序收紧。 | `e10922a`、`30484a2` | machine prereg/validator 仍固定旧三 reward/full `2^3`，待同步；无 recovery training 或连续 Gate3/Gate3B。 |
| 2026-07-13 | 新动作有效性改按“触球流形 × 适配题族 × 合法整轨站位”判断 | franco | Codex | 冻结站位 `0/64` 只能说明旧题未覆盖新动作，不能判动作无效；拉球、挡球必须用各自题型，离线安全只授予训练资格。 | `30484a2` | B/C 仍是重定位候选；无新 screen、训练或回球行为结果。 |
| 2026-07-12 | model-4000 matched paper 有了可审计 activation consumer | franco | Codex | 启动合同要求四 checkpoint 和两 Pod audit 同时满足，禁止单 Pod 或换题表绕过 barrier。 | `7bc6d1f` | consumer 只有机制证据；仍缺行为判卷和新分。 |
| 2026-07-12 | ball-physics 独立 reference oracle 恢复 | yikang | Codex | NumPy fit-lineage reference 加回 normal 校验与 source SHA，Torch contact/flight/landing 重新有独立对拍腿。 | `3df6ff5` | 只验证 physics code，不证明 policy 回球质量。 |
| 2026-07-12 | shadow solver 接入口 default-off 合入 | yikang | Claude | 加入 fail-closed backend interface、Echo 负控和 time-budget 诊断；关闭时不改变生产命令。 | `7e31819` | Torch solver body、warm start、omega 语义、runtime acceptance 未完成。 |
| 2026-07-12 | evaluator-owned same-paper contract 取代历史混分 | franco | Codex | immutable schedule、all-attempt denominator、exact motion/bank/checkpoint binding、train/exam 分离，防止不同题和幸存者分母互比。 | `83d7d56`（`f457ca3`） | 历史模型仍 inexact；physical instrument 仍不一致。 |
| 2026-07-11 | T1 event timing 变成 atomic state machine | franco | Codex | accepted strike 后原子揭题；miss 也消耗机会；robot/action/history/noise 全 carry。 | `be5d7cf`（随 Phase-1 infrastructure 合入） | 无 materialized schedule 或连续双引擎卷。 |
| 2026-07-11 | BankExam 统一 physical racket point 与 rigid-point velocity | franco | Codex | 旧 evaluator 对拍点和刚体点速度定义不一致，现把位置、角速度贡献和逐题合同绑定到同一物理拍点。 | `d4603d4` | 双引擎 physical instrument 和 mount 标定仍未闭合。 |
| 2026-07-11 | 部署 finite、fast-math 与 halt 安全修复 | franco | Codex | 移除会绕过 finite 检查的 fast-math，并让异常/停止路径 fail-closed。 | `a622997` | 不可抢占 backend send/ACK 和真机安全门仍是 G07 blocker。 |

## 重要未结论：Isaac–MuJoCo gap 还没有修好

同题工作已经定位出若干可复现差异和候选来源：racket-center tracking 不同、Isaac analytic face
normalization 会抹掉 signed-face 错误、缺 post-contact physical truth、历史 plant 单位错配。
目前只能写**部分差异已定位，整体因果归因与修复均未闭合**；没有 main 证据证明同一个修后
checkpoint 已在两个 physical instrument 上一致。真正关闭必须同时具备 semantics-correct、已标定的
plant，绑定一致的 runtime/observation/action/termination，以及冻结的两引擎 × physical/analytic 卷和
current Gate3/Gate3B 行为证据，并写明人类责任人、因果修复和 main commit。

## 基础里程碑

| 日期 | 里程碑 | 人类贡献者 | 价值 | main 证据 | 限制 |
| --- | --- | --- | --- | --- | --- |
| 2026-07-02 | A3 第一次 sim-to-real swing | yikang、jiayi | 证明 Route-A C++ 部署链能执行学习到的挥拍，真机行为接近 MuJoCo。 | `0bc9c53` | 仅正手、175-D legacy policy、scripted target；不是当前质量基线。 |
